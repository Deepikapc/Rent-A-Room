<%= form_for(@city) do |f| %>
  <% if @city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% @city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
    <p id = "nameError"></p>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<script>
  nameHandle = document.getElementById('city_name');
  nameErrorHandle = document.getElementById('nameError');

    nameHandle.onblur = function(){
      if(nameHandle.value.length == 0){
        nameErrorHandle.innerHTML = "Can't be blank";
      } else {
        nameErrorHandle.innerHTML = "";
      }
    }
</script>